@model IEnumerable<Wardrobe.Models.Top>

@{
    ViewBag.Title = "Index";
}
<body class="mainpages" background="https://static.pexels.com/photos/172292/pexels-photo-172292.jpeg>">

    <h2>Tops</h2>

    <p>
        @Html.ActionLink("Add Item", "Create")
    </p>

    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-4">
                <div class="thumbnail mainpages">
                    <div class="caption">
                        <h3>@Html.DisplayFor(modelItem => item.TopName)</h3>
                        <div class="row">
                            <div class="container-fluid">
                                <img src="@Html.DisplayFor(modelItem=>item.TopPhoto)" style="width:100%; height:100%; max-height:initial;" />
                            </div>
                        </div>
                        @*<p>@Html.DisplayFor(modelItem => item.Summary)</p>
                            <h5>
                                @Html.DisplayFor(modelItem => item.Category.Name)
                            </h5>*@
                        @*@Html.ActionLink("Read More", "Details", new { id = item.ID }, new { @class = "btn btn-primary", role = "button" })*@
                    </div>
                    @Html.ActionLink("Details", "Details", new { id = item.TopID }, new { @class = "btn btn-primary", role = "button" })
                </div>
            </div>
        }
    </div>

    @*<table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.TopName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.TopPhoto)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.TopType)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Color.ColorName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Occasion.OccasionName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Season.SeasonName)
                </th>
                <th></th>
            </tr>

        @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.TopName)
                </td>
                <td>
                    <img src="@Html.DisplayFor(modelItem => item.TopPhoto)" />
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TopType)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Color.ColorName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Occasion.OccasionName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Season.SeasonName)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id=item.TopID }) |
                    @Html.ActionLink("Details", "Details", new { id=item.TopID }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.TopID })
                </td>
            </tr>
        }

        </table>*@
    </body>